import React from 'react';
import PropTypes from 'prop-types';

import './Toolbar.scss';

const Toolbar = () => {
    return (
        <div className="Toolbar">
            <h1>Toolbar</h1>
        </div>
    );
};

Toolbar.propTypes = {
    
};

export default Toolbar;